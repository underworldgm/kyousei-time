# きょうせいタイム 開発ガイド

## プロジェクト概要

子どもの歯科矯正器具の装着時間を記録する、日本語のモバイル向けWebアプリです。
デザインは明るく親しみやすい雰囲気で、設定・時間・カレンダーの3画面があります。

## 技術構成

- ビルド不要のHTML/CSS/JavaScript
- エントリーポイント: `index.html`
- スタイル: `styles.css`
- 状態管理と画面操作: `app.js`
- 外部ライブラリやパッケージ依存なし
- データはブラウザの `localStorage` に保存

ローカル確認はリポジトリ直下で次を実行し、表示されたURLをブラウザで開きます。

```bash
python3 -m http.server 8000
```

Pythonがない場合は `npx serve .` でも確認できます。

## 実装済み機能

- 1日の目標装着時間を1〜24時間で設定
- 通知のオン・オフとブラウザ通知権限の要求
- 装着開始、一時停止、再開
- 一時停止中の時間を装着時間から除外
- 今日の累計、残り時間、進捗リング、終了予定時刻の表示
- 目標到達時の記録保存、画面内メッセージ、ブラウザ通知
- 月間カレンダー、前月・翌月移動、日別記録表示
- 目標達成日のスタンプ表示
- 今日の日付の強調表示
- タブを閉じても設定、記録、進行中セッションを復元

## 重要な仕様

- 保存キーは `kyousei-time-state-v1`。
- 日付キーはローカル時刻の `YYYY-MM-DD`。
- 実行中セッションは `activeSession` に保存する。
- `accumulatedMs` は一時停止までに積算した時間。
- `start` は現在の連続装着区間の開始時刻。一時停止中は `null`。
- `pausedAt` が値を持つ場合は一時停止中。
- カレンダーの今日の記録には、保存済み時間と進行中セッション時間の両方を含める。
- 通知はHTTPSまたはlocalhostでの利用を前提とする。`file://` では制限される場合がある。

## UI方針

- 基準となる見た目は、ユーザー提供のiPhone向けモックアップ。
- モバイル画面で縦スクロールをできるだけ発生させない。
- 上部の歯のキャラクターを隠さない。
- 子ども向けの配色と大きな操作領域を保つ。
- 既存の3画面構成と下部ナビゲーションを維持する。
- 文字や操作部品が重ならないか、小さい画面でも必ず確認する。

## 変更時の確認

1. `node --check app.js` でJavaScript構文を確認する。
2. スマートフォン相当の幅と高さで3画面を目視確認する。
3. 開始 → 一時停止 → 再開の順で時間が正しく積算されることを確認する。
4. 再読み込み後も進行中または一時停止中の状態が復元されることを確認する。
5. カレンダーで今日、選択日、達成スタンプが区別できることを確認する。

## Git運用

- リモート: `https://github.com/underworldgm/kyousei-time.git`
- 既定ブランチ: `main`
- 作業前に `git pull --ff-only` を実行する。
- 既存のユーザー変更を消さない。
- 機能単位でコミットし、確認後に `main` へpushする。

## 現在の状態

初期版は実装済みです。次の作業では、まず実機サイズでの表示とタイマー操作を確認し、ユーザーから追加要望がある箇所だけを変更してください。
